version: '3.8'

services:
  web:
    build: .
    image: quizit-backend:latest
    container_name: QuizIT-Backend
    ports:
      - "${APP_PORT}:${APP_PORT}"
    volumes:
      - .:/app
    environment:
      - NODE_ENV=production
